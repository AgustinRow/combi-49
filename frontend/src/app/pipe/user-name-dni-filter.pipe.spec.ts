import { UserNameDniFilterPipe } from './user-name-dni-filter.pipe';

describe('UserNameDniFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UserNameDniFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
